"""LuckyEngine integration helpers (sweep runners, backends, contract checks).

This package is intentionally lightweight: it provides a small backend interface
and a mock backend so you can validate LeRobot policy↔processor contracts without
requiring LuckyEngine/Hazel gRPC stubs in this repo.
"""


